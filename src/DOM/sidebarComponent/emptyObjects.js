const noCreatedTasksMessage = document.createElement("p");
noCreatedTasksMessage.textContent = "No Created Tasks";

const noCreatedListsMessage = document.createElement("p");
noCreatedListsMessage.textContent = "No Created Lists";

const noAddedTasksMessage = document.createElement("p");
noAddedTasksMessage.textContent = "No Added Tasks";

const noTasksToAddMessage = document.createElement("p");
noTasksToAddMessage.textContent = "No Tasks to Add";

const cantAddToDefaultMessage = document.createElement("p");
cantAddToDefaultMessage.textContent = "Can't Add to Default Lists";

const noCurrentTasksMessage = document.createElement("p");
noCurrentTasksMessage.textContent = "No Current Tasks";

export {
  noCreatedTasksMessage,
  noCreatedListsMessage,
  noAddedTasksMessage,
  noTasksToAddMessage,
  cantAddToDefaultMessage,
  noCurrentTasksMessage,
};
